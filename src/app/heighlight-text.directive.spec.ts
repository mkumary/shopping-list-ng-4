import { HeighlightTextDirective } from './heighlight-text.directive';

describe('HeighlightTextDirective', () => {
  it('should create an instance', () => {
    const directive = new HeighlightTextDirective();
    expect(directive).toBeTruthy();
  });
});
